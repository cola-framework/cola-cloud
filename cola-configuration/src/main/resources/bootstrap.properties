spring.application.name=configuration
eureka.instance.non-secure-port=${server.port:8080}
eureka.instance.metadata-map.instance=${spring.application.name}:${random.value}
eureka.client.service-url.default-zone=http://${eureka.host:localhost}:${eureka.port:8761}/eureka/